import React, { useState } from 'react';


function Velocidad() {



        let { distancia, tiempo, velocidad, lista } = this.state;
        velocidad = Number(distancia) / Number(tiempo);

        return (
            <>
               

            </>
        )



}

export default Velocidad;